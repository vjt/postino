# postino-managed: domain → transport
hosts = {{ ctx.db_host }}
user = {{ ctx.db_user }}
password = {{ ctx.db_password.get_secret_value() }}
dbname = {{ ctx.db_name }}
query = SELECT transport FROM domain WHERE domain='%s' AND active = '1'
