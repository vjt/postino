# Architecture-test allowlist
#
# Keyed by `<test_id>` -> `<relpath>` -> `<lineno>` = `"WHY: <reason>"`.
# Entries here suppress a single (test, file, line) violation.
#
# Rules:
# - Every entry MUST start with `WHY:` followed by a one-line reason.
# - Renaming or moving a file invalidates its allowlist entries; a fresh
#   justification must be provided.
# - Prefer fixing violations over allowlisting; this file is the
#   compromise of last resort.

[test_no_any]
[test_no_any."src/postino_core/output.py"]
27 = "WHY: tmp local that mirrors model_dump's untyped JSON return; fix lands in PR-A6 when Renderer moves to postino/."
46 = "WHY: row cell is the JSON payload of a Pydantic field via getattr — typing it would require a structural Protocol that JSON has no equivalent for; fix in PR-A6."

[test_no_any."src/postino_core/password.py"]
18 = "WHY: passlib's hashers expose no public Protocol; PR-A5 wraps them behind a typed PasswordHasher Protocol."

[test_no_cast]
[test_no_cast."src/postino/commands/alias.py"]
58 = "WHY: Renderer.render's `list[BaseModel]` arg is invariant; concrete-model lists need a cast until Renderer.render is generified — fix in PR-A6 alongside the postino_core->postino move."

[test_no_cast."src/postino/commands/domain.py"]
76 = "WHY: same Renderer.render variance issue as alias.py:58 — fix in PR-A6."

[test_no_cast."src/postino/commands/quota.py"]
34 = "WHY: same Renderer.render variance issue — fix in PR-A6."
37 = "WHY: same Renderer.render variance issue — fix in PR-A6."

[test_no_cast."src/postino/commands/user.py"]
112 = "WHY: same Renderer.render variance issue — fix in PR-A6."

[test_no_cast."src/postino_core/password.py"]
35 = "WHY: passlib's hash()/verify() return Any; PR-A5 introduces a typed PasswordHasher Protocol replacing the cast."
53 = "WHY: same — passlib returns Any, fixed in PR-A5 by Protocol wrapper."

[test_type_ignore_justified]
# Most pre-existing ignores are SQLAlchemy `RowMapping` access returning Any
# (documented in CLAUDE.md). PR-A5 wraps passlib; PR-A6 replaces typer's
# `ctx.obj` dict with a typed CliState. Until then snapshot here.

[test_type_ignore_justified."src/postino/cli.py"]
75 = "WHY: pydantic-settings raises ValidationError for missing fields; pyright thinks PostinoSettings() is missing args. Captured in PR-A6 cleanup."

[test_type_ignore_justified."src/postino/commands/alias.py"]
18 = "WHY: typer Context.obj is dict[str, Any]; PR-A6 introduces a typed CliState dataclass."

[test_type_ignore_justified."src/postino/commands/check.py"]
13 = "WHY: typer Context.obj is dict[str, Any]; PR-A6 typed CliState."

[test_type_ignore_justified."src/postino/commands/domain.py"]
20 = "WHY: typer Context.obj is dict[str, Any]; PR-A6 typed CliState."

[test_type_ignore_justified."src/postino/commands/quota.py"]
18 = "WHY: typer Context.obj is dict[str, Any]; PR-A6 typed CliState."

[test_type_ignore_justified."src/postino/commands/status.py"]
14 = "WHY: typer Context.obj is dict[str, Any]; PR-A6 typed CliState."

[test_type_ignore_justified."src/postino/commands/user.py"]
34 = "WHY: typer Context.obj is dict[str, Any]; PR-A6 typed CliState."
182 = "WHY: Renderer.render is invariant on list[BaseModel]; PR-A6 generifies it."

[test_type_ignore_justified."src/postino_core/password.py"]
12 = "WHY: passlib lacks a typed __init__; PR-A5 wraps via PasswordHasher Protocol."

[test_type_ignore_justified."src/postino_core/services/alias.py"]
86 = "WHY: SQLAlchemy RowMapping access yields Any; documented project-wide."
110 = "WHY: SQLAlchemy RowMapping access yields Any."
114 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
115 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
116 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
117 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
118 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
119 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."

[test_type_ignore_justified."src/postino_core/services/domain.py"]
74 = "WHY: SQLAlchemy RowMapping access yields Any."
87 = "WHY: SQLAlchemy RowMapping access yields Any."
93 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
94 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
95 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
96 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
97 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
98 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
99 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
100 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
101 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."

[test_type_ignore_justified."src/postino_core/services/mailbox.py"]
138 = "WHY: SQLAlchemy RowMapping access yields Any."
210 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
213 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
214 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
215 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
252 = "WHY: SQLAlchemy RowMapping access yields Any."

[test_type_ignore_justified."src/postino_core/services/quota.py"]
23 = "WHY: SQLAlchemy Row._mapping attribute access not in stubs."
25 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
26 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
27 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
36 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
37 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."
38 = "WHY: SQLAlchemy RowMapping[str, Any] indexing."

[test_no_bare_dict]
[test_no_str_dispatch]
[test_no_empty_str_sentinel]
[test_no_empty_str_sentinel."src/postino/commands/quota.py"]
26 = "WHY: typer Argument default; PR-A6 swaps for `username: str | None = None` plus typer.Argument(None) per CLI cleanup."

[test_pydantic_strict]
[test_no_secret_repr]
