"""postinod test session configuration."""

from __future__ import annotations

# pytest-asyncio is configured via pyproject.toml [tool.pytest.ini_options].
# Module exists as a marker so future shared fixtures (DB, JWKS stubs) live here.
